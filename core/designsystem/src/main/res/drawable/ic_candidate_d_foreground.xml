<?xml version="1.0" encoding="utf-8"?>
<!--
  CANDIDATE launcher mark, for review only. Not wired into ic_launcher.xml.

  The tick alone, as big as the safe zone allows. Unmistakable at any size and the only
  candidate that is still legible as a favicon - but it says 'approved', not 'phone', so it
  leans entirely on the app name to carry the meaning.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path android:strokeColor="#22C55E" android:strokeWidth="15"
        android:strokeLineCap="round" android:strokeLineJoin="round"
        android:fillColor="#00000000"
        android:pathData="M30,57l16,17l32,-40" />
</vector>
